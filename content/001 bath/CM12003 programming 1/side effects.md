#haskell 
in [[haskell]], a *side effect* is a action that a computer needs to take aside from running through a program. this could include, for example:
+ input/output (`IO`)
+ random number generation
+ networking