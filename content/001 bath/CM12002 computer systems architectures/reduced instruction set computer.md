---
aliases:
  - RISC
---
[[reduced instruction set computer|RISC]] requires more simple and straightforward instructions, where each instruction only requires a single clock cycle to complete. however, as this requires more lines of code to code the same functionality, more memory is needed to store programs. additionally, [[compiler]]s need to be more complex to convert high-level languages into these simple instructions, which can increase the development time and cost.