---
aliases:
  - CISC
---
[[complex instruction set computer|CISC]] instruction sets are designed to be large and more complex (take multiple clock cycles to execute), which allows for simple [[compiler]]s and programs which take less memory. it also allows for memory-to-memory operations.